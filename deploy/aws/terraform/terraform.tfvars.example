ami_id        = "ami-0123456789abcdef0"  # Replace with latest from GitHub Releases
aws_region    = "us-east-1"
instance_type = "t3.medium"
key_name      = "my-key-pair"

# Optional: restrict SSH access to your IP
# allowed_ssh_cidrs = ["203.0.113.0/32"]

# Optional: custom domain with auto-SSL
# domain      = "artifacts.example.com"
# admin_email = "admin@example.com"

# Optional: use an existing VPC/subnet
# vpc_id    = "vpc-abc123"
# subnet_id = "subnet-abc123"

# Artifact storage volume size in GB
# data_volume_size = 50
